% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/ref_kmers.R
\name{dense_frequencies_matrix}
\alias{dense_frequencies_matrix}
\alias{dense_frequencies_matrix.cfdnalab_global_ref_kmer_frequencies}
\alias{dense_frequencies_matrix.cfdnalab_windowed_ref_kmer_frequencies}
\alias{dense_frequencies_matrix.cfdnalab_grouped_ref_kmer_frequencies}
\title{Return reference k-mer frequencies as a dense matrix.}
\usage{
dense_frequencies_matrix(x, ...)

\method{dense_frequencies_matrix}{cfdnalab_global_ref_kmer_frequencies}(
  x,
  allow_densify = FALSE,
  motifs = NULL,
  motif_idxs = NULL,
  ...
)

\method{dense_frequencies_matrix}{cfdnalab_windowed_ref_kmer_frequencies}(
  x,
  allow_densify = FALSE,
  window_idxs = NULL,
  motifs = NULL,
  motif_idxs = NULL,
  ...
)

\method{dense_frequencies_matrix}{cfdnalab_grouped_ref_kmer_frequencies}(
  x,
  allow_densify = FALSE,
  groups = NULL,
  group_idxs = NULL,
  motifs = NULL,
  motif_idxs = NULL,
  ...
)
}
\arguments{
\item{x}{A cfDNAlab reference k-mer object.}

\item{...}{Method-specific arguments.}

\item{allow_densify}{If \code{TRUE}, allow sparse output to be converted to a
zero-filled dense matrix in memory. Zero filling uses the selected motifs
from \code{motifs(x)}, not every possible k-mer unless \code{all_motifs(x)} is
\code{TRUE}. Sparse output errors by default.}

\item{motifs}{Optional motif label vector. Use either \code{motifs} or
\code{motif_idxs}, not both.}

\item{motif_idxs}{Optional one-based motif index vector.}

\item{window_idxs}{Optional one-based window index vector for windowed output.}

\item{groups}{Optional group name vector for grouped output. Use either
\code{groups} or \code{group_idxs}, not both.}

\item{group_idxs}{Optional one-based group index vector for grouped output.}
}
\value{
A dense numeric matrix.
}
\description{
Sparse output stores only non-zero frequencies. This method does not create
a zero-filled dense matrix from sparse output unless it explicitly receives
\code{allow_densify = TRUE}. Densifying fills zeroes only across the motif axis
returned by \code{motifs(x)}.
}

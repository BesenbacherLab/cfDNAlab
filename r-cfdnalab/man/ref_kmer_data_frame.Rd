% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/ref_kmers.R
\name{ref_kmer_data_frame}
\alias{ref_kmer_data_frame}
\alias{ref_kmer_data_frame.cfdnalab_global_ref_kmer_frequencies}
\alias{ref_kmer_data_frame.cfdnalab_windowed_ref_kmer_frequencies}
\alias{ref_kmer_data_frame.cfdnalab_grouped_ref_kmer_frequencies}
\title{Return reference k-mer frequencies and reconstructed counts as a data frame.}
\usage{
ref_kmer_data_frame(x, ...)

\method{ref_kmer_data_frame}{cfdnalab_global_ref_kmer_frequencies}(x, densify = FALSE, motifs = NULL, motif_idxs = NULL, ...)

\method{ref_kmer_data_frame}{cfdnalab_windowed_ref_kmer_frequencies}(
  x,
  window_idxs = NULL,
  densify = FALSE,
  motifs = NULL,
  motif_idxs = NULL,
  max_blacklisted_fraction = 1,
  ...
)

\method{ref_kmer_data_frame}{cfdnalab_grouped_ref_kmer_frequencies}(
  x,
  groups = NULL,
  group_idxs = NULL,
  densify = FALSE,
  motifs = NULL,
  motif_idxs = NULL,
  max_blacklisted_fraction = 1,
  ...
)
}
\arguments{
\item{x}{A cfDNAlab reference k-mer object.}

\item{...}{Method-specific selection arguments.}

\item{densify}{If \code{TRUE}, sparse outputs add explicit zero-frequency rows
for selected observed motifs. Dense outputs ignore this option.}

\item{motifs}{Optional motif label vector. Use either \code{motifs} or
\code{motif_idxs}, not both.}

\item{motif_idxs}{Optional one-based motif index vector.}

\item{window_idxs}{Optional one-based window index vector for windowed output.}

\item{max_blacklisted_fraction}{Maximum row \code{blacklisted_fraction} in 0..1
to retain before returning values. The default \code{1.0} keeps all selected
rows.}

\item{groups}{Optional group name vector for grouped output. Use either
\code{groups} or \code{group_idxs}, not both.}

\item{group_idxs}{Optional one-based group index vector for grouped output.}
}
\value{
A data frame containing row metadata, motif metadata, \code{frequency},
and reconstructed \code{count}.
}
\description{
Sparse outputs return stored non-zero frequency rows unless the method
explicitly receives \code{densify = TRUE}. Densifying adds explicit zero-frequency
rows for selected observed motifs. Dense outputs always include zeroes.
}
